import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const AnimatableView = ({ children, style, type }) => {
  return <View>{children}</View>;
};

export default AnimatableView;

const styles = StyleSheet.create({});
